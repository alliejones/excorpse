<h1>Game #<%= @game.id %></h1>
<h2>Made by <%= @game.users.pluck(:email).to_sentence %></h2>

<%= image_tag @game.image %>

<%= link_to 'Edit', edit_game_path(@game) %> |
<%= link_to 'Back', games_path %>
