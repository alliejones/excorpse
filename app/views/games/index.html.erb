<h1>Games</h1>
<h2>Finished Games</h2>
<ul class="thumbnails">
<% @games.each do |game| %>
  <li class="span3">
    <div class="thumbnail">
      <%= link_to image_tag(game.image :thumb), game %>
    </div>
  </li>
<% end %>
</ul>

<%= link_to 'New Game', new_game_path, :class => 'btn' %>
